# Directorio `routes`

Contiene la definición de todas las rutas (endpoints) de la API.

El archivo `api.php` mapea las URLs y los verbos HTTP (GET, POST, PUT, DELETE) a los métodos de los controladores correspondientes.

Centralizar las rutas aquí ofrece una visión clara y organizada de toda la superficie de la API.