# Directorio `shared_widgets`

Contiene widgets que son **genéricos y reutilizables en múltiples funcionalidades** de la aplicación.

A diferencia de los widgets dentro de una feature, estos no tienen dependencias de ninguna funcionalidad específica.

**Ejemplos:**
- `CustomButton.dart`
- `LoadingSpinner.dart`
- `FormField.dart`