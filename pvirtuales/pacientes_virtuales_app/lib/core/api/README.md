# Directorio `api`

Contiene la configuración del cliente HTTP (ej. Dio). Aquí se definen los interceptores para añadir tokens de autenticación a las cabeceras, gestionar errores de red de forma centralizada y configurar la URL base de la API.